//! Neural-network building blocks (Linear, activations, sequential containers).
//!
//! Stub owned by the `nn` workstream. Build on the frozen `Tensor` API from
//! `crate::tensor` / `crate::ops`: `matmul`, `add` (broadcasts a bias row),
//! `relu`, `sigmoid`, and the autograd `backward()` are all available.
